2) Neue Art der Wire ziehen (durch Punkte markieren mit klick)
7) Beim Klick auf wire soll es Funktionen Löschen, Farbe ändern, Querschnitt ändern, Kabelbreite ändern (Zeichnung) zusätzlich für das ganze elementare Netz (layer elementary aus checks) geben. Am besten vermutlich erst im Toolbox nur ein button "Netz wählen" und erst wenn man drauf klickt, dann erscheint das gesamte Netz als selektiert (ggf. kann bereits vorhandene highlight für Netze aus checks genutzt werden) und dann Funktionen für das gesamte Netz zeigen.
9) [DONE] Aktuell is es versucht die Breite der Sidebar so zu gestalten, dass dort zwei Komponenten nebeneinander erscheinen. Das funktioniert auch in Firefox. In Chrome aber wird nur eine Komponente pro zeile angezeigt und der Rest der Fläche rechts davon ist leer.
11) Setup testing concept...
12) Handling von angebundenen wires beim Drehen der Komponente
3) [DONE] Tooltip bei bewegen der Mouse über Pin: adaptive Breite
4) [DONE] Aktuell, wenn Wire bewegt wird, verschwindet ToolBox. Das ist gut so. Das gleiche soll für das Verschieben der Komponente implementiert werden. Außerdem sollen dabei beim Verschieben auch für die Eingabefelder der Komponente Änderungsmodus deaktiviert werden.
5) [DONE] wenn Multiselect tool aktiviert ist, dann passendere kursor anzeigen. Aktuell ein Hand mit Finger.
6) [DONE] Multiselect wird aktiviert und deaktiviert wieder erst wenn etwas ausgewählt wurde. Soll zusätzlich deaktivierbar sein über den gleichen button und status ob aktiv oder nicht soll im Button ersichtlich sein.
10) [DONE] Wenn diagramm leer ist, erscheint "Beginne mit dem Ziehen von Komponenten auf die Arbeitsfläche oder lade ein Beispiel". Dabei soll dazu noch direkt hier die Auswahlmöglichkeit erscheinen ein Beispiel zu laden. Ansonste muss der neue Nutzer es erst in Sidebar suchen.
1) [DONE] Kreuzung zwei wires, radius adaptiv
8) [DONE] Die Position von Toolbox beim Netzauswahl ist aktuell statisch und kann außerhalb vom sichbaren Bereich liegen beim Zoom. Es soll dynamisch sein und so liegen, dass sie nicht die Bedienelemente vom Wire (Eckpunkte, Mittelpunkt) überdeckt. Aktuell weil es überdeckungen möglich, ist toolbox auch nich tganz vorne. dann kann es ganz forne in Z liegen.
